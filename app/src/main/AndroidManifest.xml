<?xml version="1.0" encoding="utf-8"?>
<!--
    MoRead 隐私红线：本清单不含任何权限声明。
    禁止添加 INTERNET / 存储 / 相机等任何权限；CI 会断言权限标签数量为 0。
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        AndroidX Core 会向合并清单注入应用内签名级权限
        ${applicationId}.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION。
        本应用不注册动态广播接收器，按 PRD 零权限红线显式移除该内部权限声明。
    -->
    <permission android:name="${applicationId}.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION" tools:node="remove" />
    <uses-permission android:name="${applicationId}.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION" tools:node="remove" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MoRead">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:configChanges="uiMode|orientation|screenSize|keyboardHidden"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            外部「打开方式 / 分享」入口：text/* 文档，scheme 仅 content / file。
            第三方传入异常 URI 时 ReaderActivity 会统一捕获并展示错误页。
        -->
        <activity
            android:name=".ui.reader.ReaderActivity"
            android:configChanges="uiMode|orientation|screenSize|keyboardHidden"
            android:exported="true"
            android:theme="@style/Theme.MoRead.Reader">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="text/plain" />
                <data android:mimeType="text/markdown" />
                <data android:mimeType="text/x-markdown" />
                <data android:mimeType="text/*" />
            </intent-filter>
        </activity>

        <!--
            编辑页：仅应用内显式启动，不导出；不新增任何权限。
            编辑文件读写在用户对单个 SAF URI 的授权范围内完成。
        -->
        <activity
            android:name=".ui.editor.EditorActivity"
            android:configChanges="uiMode|orientation|screenSize|keyboardHidden"
            android:exported="false"
            android:theme="@style/Theme.MoRead.Reader"
            android:windowSoftInputMode="adjustResize" />

    </application>
</manifest>
